# Database Configuration
USE_POSTGRESQL=False

# PostgreSQL Settings (only used if USE_POSTGRESQL=True)
DB_NAME=school_management_db
DB_USER=postgres
DB_PASSWORD=postgres123
DB_HOST=localhost
DB_PORT=5432
