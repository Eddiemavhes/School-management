# Database Configuration
# Options: sqlite (default), postgresql, mysql
USE_SQLITE=True
USE_POSTGRESQL=False
USE_MYSQL=False

# MySQL Settings (only used if USE_MYSQL=True)
DB_NAME=school_management_db
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# PostgreSQL Settings (only used if USE_POSTGRESQL=True)
# DB_NAME=school_management_db
# DB_USER=postgres
# DB_PASSWORD=postgres123
# DB_HOST=localhost
# DB_PORT=5432
